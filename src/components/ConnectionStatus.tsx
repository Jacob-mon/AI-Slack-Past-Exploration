import React from 'react';

/**
 * 이 컴포넌트는 현재 사용되지 않지만,
 * 손상된 파일로 인한 빌드 오류를 방지하기 위해 유효한 컴포넌트로 유지됩니다.
 */
const ConnectionStatus: React.FC = () => {
  return null;
};

export default ConnectionStatus;
